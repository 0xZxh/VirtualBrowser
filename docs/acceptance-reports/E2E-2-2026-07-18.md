# E2E-2 验收报告 — Agent-E2E-2 — 2026-07-18

## 范围

- **任务 ID：** E2E-2（Setup 实机 + 生产路径联调）
- **认领表：** [`AGENT_COORDINATION.md`](../AGENT_COORDINATION.md) → E2E-2 **done**（部分通过，见阻塞）
- **目标链路：** `Setup.exe` → 桌面窗口 → 登录云端 `http://120.78.76.171:3001` → 创建/启动指纹环境
- **变更文件：**
  - 新建本报告
  - 修补 `packaging/scripts/build-client.ps1`（staging 复制 `config/vb-paths.js`，否则 cross-platform-prep 后 require 失败）
  - 更新 `docs/ACCEPTANCE.md` §S6/S7 与总进度
  - 更新 `docs/AGENT_COORDINATION.md`
  - 更新 `docs/acceptance-reports/README.md`
  - 产物：重打 `packaging/output/VirtualBrowser-Setup-0.1.0.exe`（含生产 CloudApiBase + UI 内嵌 baseURL）

## 环境

| 项 | 值 |
|----|-----|
| 日期 | 2026-07-18 |
| OS | Windows 10 |
| 云端 API | `http://120.78.76.171:3001`（mongo connected） |
| Compat | `http://120.78.76.171:9000`（远程，无本机内核同进程） |
| NSIS | `D:\NSIS\makensis.exe` v3.12 |
| Setup | `packaging/output/VirtualBrowser-Setup-0.1.0.exe`（本轮重打） |
| 安装目录 | `%LOCALAPPDATA%\Programs\VirtualBrowser` |

## 前置检查

### A — Setup.exe 存在

- **命令：** `Test-Path packaging/output/VirtualBrowser-Setup-0.1.0.exe`
- **实际：** `True`（验收前为 2026-07-14 旧包，内嵌 `127.0.0.1`；本轮已重打）
- **结果：** ☑ 通过

### B — config/client.json

- **命令：** `Get-Content config/client.json`
- **实际：**
  ```json
  {
    "cloudApiBase": "http://120.78.76.171:3001",
    "windowTitle": "VirtualBrowser",
    "productVersion": "0.1.0"
  }
  ```
- **结果：** ☑ 通过

### C — Setup 内嵌地址 / 重打

- **发现：** 旧 staging `packaging/staging-20260714214208/config/client.json` 仍为 `http://127.0.0.1:3001`
- **首次重打（`-SkipUiBuild`）失败原因：** `profile-sync.js` require `../../config/vb-paths`，staging 未复制 → 已修 `build-client.ps1`
- **完整重打命令：**
  ```powershell
  $env:Path = "D:\NSIS;" + $env:Path
  cd D:\bytesio\VirtualBrowser\packaging\scripts
  .\build-client.ps1 -CloudApiBase "http://120.78.76.171:3001"
  ```
- **实际：** Setup 生成成功；`dist/server/static/js/app.js` 内嵌 `baseURL:"http://120.78.76.171:3001"`（**不可**仅用 `-SkipUiBuild`，否则 UI 仍为 `/prod-api`）
- **产物大小 / 时间：** 486485530 bytes，2026/7/18 16:40:56
- **是否重打 Setup：** ☑ **是**（相对 07-14 旧包）

### D — health

- **命令：** `curl.exe -s http://120.78.76.171:3001/health`
- **实际：**
  ```json
  {"ok":true,"service":"virtualbrowser-backend","stack":"nestjs","storage":"mongo","mongo":"connected"}
  ```
- **结果：** ☑ 通过

## 验证记录

### 检查项 1 — 静默安装 Setup.exe

- **命令：**
  ```powershell
  Start-Process -FilePath "packaging\output\VirtualBrowser-Setup-0.1.0.exe" -ArgumentList "/S" -Wait
  ```
- **预期：** 安装到 `%LOCALAPPDATA%\Programs\VirtualBrowser`；桌面快捷方式；client.json 为生产地址
- **实际：** `setup_exit=0`；目录存在；`config/client.json` → `http://120.78.76.171:3001`；`config/vb-paths.js` 存在；桌面与开始菜单快捷方式存在
- **结果：** ☑ 通过

### 检查项 2 — 桌面窗口启动（非系统浏览器标签）

- **命令：** 启动 `%LOCALAPPDATA%\Programs\VirtualBrowser\VirtualBrowser.exe`（加 `--remote-debugging-port=9222` 便于探测）
- **预期：** 独立 Electron 桌面窗口
- **实际：**
  - 进程路径：`...\Programs\VirtualBrowser\VirtualBrowser.exe`
  - 主窗口标题：`列表 - 指纹浏览器控制台`（CDP `title`）
  - CDP url：`file:///.../dist/server/index.html#/index`（**非**系统 Chrome 标签）
- **结果：** ☑ 通过

### 检查项 3 — 登录云端 admin / admin123

- **命令（API）：**
  ```powershell
  curl.exe -s -X POST "http://120.78.76.171:3001/auth/login" -H "Content-Type: application/json" --data-binary "@login.json"
  # body: {"username":"admin","password":"admin123"}
  ```
- **实际（API）：** `{"code":0,"data":{"token":"…","user":{"username":"admin",…}}}`
- **实际（桌面 GUI）：** CDP 探测页面已在「列表」路由；`localStorage Admin-Token` 存在；`fetch` 云端 `/api/environments` → `code:0`，表格含 `e2e2-20260718`；**本轮未重新在登录表单键入账号**（沿用已登录态 + 云端列表已证明客户端可连生产 API）
- **结果：** ☑ API 登录通过 · ☑ 桌面已连云端列表 · ☐ 登录表单重新录入未做（非阻塞）

### 检查项 4 — 创建环境 → 启动指纹窗口

- **创建（API）：**
  ```text
  POST /api/environments → {"code":0,"data":{"id":1,"name":"e2e2-20260718",…}}
  ```
- **启动：** 安装目录内 `native-runtime.handleNativeCall("launchBrowser",["1"])`（带 `CLOUD_API_TOKEN`）
- **实际：**
  ```text
  launch {"ok":true,"debuggingPort":19200,"envId":"1"}
  running ["1"]
  netstat :19200 LISTENING
  CDP /json/version → "Browser": "Chrome/146.0.7680.72"
  ```
- **备注：** GUI 点「启动」时 CDP 隔离世界看不到 `chrome.send`（contextBridge）；改用同机 native-runtime 验证内核弹出。首次无 token 时进程曾很快退出；带 token 后稳定运行。
- **结果：** ☑ 通过（同机内核启动 + CDP 端口就绪）

### 检查项 5 — Compat API

- **远程探测：** `:9000` 无 key → `401 缺少 api-key`（服务存活）
- **创建 api-key：** `POST /api/api-keys` → 返回完整 `key`
- **getBrowserList（带 key）：** `success:true`，含本轮环境
- **addBrowser：** 返回 `服务器内部错误`（列表中曾出现 `e2e2-compat` id=2，行为不稳定）
- **launchBrowser：** **未在远程执行完整验收** — Compat launch 需与指纹内核同机；本轮内核在客户机，API 在 `120.78.76.171`
- **结果：** 🟡 远程鉴权/列表可用 · ☐ addBrowser/launch 未完全通过 · 注明「launch 需同机内核」

## 结果汇总表

| 项 | 结果 | 说明 |
|----|------|------|
| health | ☑ | mongo connected |
| 重打 Setup | ☑ 是 | 含 `120.78.76.171:3001` + UI baseURL |
| 静默安装 | ☑ | `/S` exit 0 |
| 桌面窗口 | ☑ | Electron「指纹浏览器控制台」 |
| API 登录 | ☑ | admin / admin123 |
| 桌面连云 | ☑ | 环境列表来自云端 |
| 创建环境 | ☑ | id=1 e2e2-20260718 |
| 启动指纹 | ☑ | debuggingPort 19200 / Chrome 146 |
| Compat 远程 launch | ☐ | 需同机内核；addBrowser 500 |

## 与文档差异

- `ACCEPTANCE.md` §S7 此前结论仍写 CloudApiBase=`127.0.0.1`；本轮已改为生产 IP 并重打安装包
- `build-client.ps1` 原先未打包 `vb-paths.js`（cross-platform-prep 后阻塞）；已补复制
- `-SkipUiBuild` **不能**用于生产 CloudApiBase 切换（UI 的 `VUE_APP_BASE_API` 须重新 `npm run build`）

## 阻塞 / 待确认

| 阻塞项 | 说明 | 建议 |
|--------|------|------|
| HTTPS | 生产仍为明文 HTTP `:3001` | 按 `CLOUD_DEPLOY.md` 上 Nginx + 证书后复验 |
| Compat 远程 addBrowser 500 | 带 key 调用返回内部错误 | 服务器侧查 Compat/Nest 日志 |
| Compat launch 同机约束 | 远程 `:9000` 无客户机 Chrome-bin | 客户机本机起 Compat，或接受仅测管理 API |
| GUI `chrome.send` CDP 不可见 | 自动化点「启动」受限 | 人工点启动做目视复核（本轮已用 native-runtime 等价验证） |
| 卸载保留用户数据 | 本轮未跑卸载 | 可选复验 ACCEPTANCE §S7 第 6 步 |
| 登录表单重录 | 本轮沿用已登录 token | 可选清 localStorage 后重登 |

## 阶段结论（E2E-2 视角）

| 阶段 | 结论 | 说明 |
|------|------|------|
| **S6** | 🟢 **公网 HTTP mongo 可达** | health/login/创建环境 OK；HTTPS 仍待运维 |
| **S7** | 🟢 **实机安装+窗口+云端+指纹启动通过** | Setup 已用生产 IP 重打；卸载未测 |
| **S8 Compat** | 🟡 **远程部分可用** | 鉴权/列表 OK；add/launch 受限 |

**整体 E2E-2：** ☐ 完全通过 · ☑ **部分通过**（客户交付主路径 Setup→窗口→云端→指纹已通；HTTPS / Compat 远程 launch 未完全通过）

**备注（2026-07-18 补）：** GUI「启动」在安装包内曾因 Electron 无法覆盖 `window.chrome` 而失败；已改为 `vbDesktop.invoke`。见 [`fix-launch-packaged-2026-07-18.md`](fix-launch-packaged-2026-07-18.md)。

## Agent 收尾 checklist

- [x] 更新 `ACCEPTANCE.md` §S6/S7
- [x] 更新 `AGENT_COORDINATION.md` → done
- [x] 更新 `acceptance-reports/README.md`
- [x] 修补 `packaging/scripts/build-client.ps1`（vb-paths）
- [ ] 未 git commit（按任务要求）
