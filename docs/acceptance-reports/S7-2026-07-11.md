# S7 验收报告 — Agent-S7 — 2026-07-11

## 范围

- **任务 ID：** S7-client
- **认领表：** [`AGENT_COORDINATION.md`](../AGENT_COORDINATION.md) → S7-client **done**
- **变更文件：**
  - 新建 `desktop-shell/`（Electron 壳 + preload `chrome.send` + native IPC 桥接）
  - 新建 `packaging/`（`build-client.ps1`、NSIS 脚本、`client.json.template`）
  - 修改 `docs/AGENT_COORDINATION.md`（认领 → 完成）

## 验证记录

### 检查项 1 — native-runtime 约束（仅 import，不重写）

- **预期：** desktop-shell 只 `require` `server/lib/native-runtime.js`，不复制 launchBrowser / handleNativeCall 等内部逻辑
- **实际：** `desktop-shell/src/native-ipc.js` 通过 `paths.js` 解析路径后 `require(nativeRuntimePath)`，唯一业务入口为 `handleNativeIpc` → `nativeRuntime.handleNativeCall`
- **结果：** ☑ 通过

### 检查项 2 — native-runtime smoke

- **命令：**
  ```powershell
  node D:\bytesio\VirtualBrowser\server\lib\__tests__\native-runtime-smoke.js
  ```
- **预期：** 模块导出、debug 端口池、getBrowserList / getBrowserVersion / getRuningBrowser 正常
- **实际：** `native-runtime-smoke: all passed`
- **结果：** ☑ 通过

### 检查项 3 — desktop-shell IPC smoke

- **命令：**
  ```powershell
  node D:\bytesio\VirtualBrowser\desktop-shell\scripts\smoke-test.js
  ```
- **预期：** 从 desktop-shell 侧 import native-runtime 并调用 `handleNativeIpc` 基础方法
- **实际：** `desktop-shell-smoke: all passed`
- **结果：** ☑ 通过

### 检查项 4 — desktop-shell 结构（Electron + preload）

- **预期：** 主进程加载 `dist/server/index.html`；preload 注入 `chrome.send`；IPC 转发至 native-runtime
- **实际：**
  - `desktop-shell/src/main.js` — BrowserWindow + `ipcMain.handle('native-call')`
  - `desktop-shell/src/preload.js` — `contextBridge.exposeInMainWorld('chrome', { send })`，回调协议与 `server/src/api/native.js` 一致
  - `desktop-shell/src/paths.js` — dev / 打包双模式路径解析
- **结果：** ☑ 通过（代码审查；**未启动 Electron 窗口目视**）

### 检查项 5 — packaging 可执行流程

- **预期：** `packaging/scripts/build-client.ps1` 编排 UI 构建、staging 组装、NSIS 安装包；卸载保留用户数据
- **实际：**
  - `build-client.ps1` 支持 `-CloudApiBase`、`-ProductVersion`、`-SkipUiBuild`、`-SkipNsis`
  - `packaging/nsis/installer.nsi` 卸载段注释保留 `%LOCALAPPDATA%\VirtualBrowser\`
  - staging 复制 Electron、`dist/server`、`Chrome-bin`、`server/lib`、`config/client.json`
- **结果：** ☑ 通过（脚本就绪；**未在本机完整跑通 build + NSIS**）

### 检查项 6 — 端到端安装包验收（ACCEPTANCE §S7）

- **预期：** Setup.exe 安装 → 桌面窗口 → 登录云端 → 创建/启动指纹环境 → 卸载保留用户数据
- **实际：** 依赖 `server` UI 构建产物、`Chrome-bin` 内核、NSIS (`makensis`)、可用云端 API；本 Agent 环境未执行完整 E2E
- **结果：** ☐ 未通过（待用户实机验收）

## 与文档差异

- 计划文件 [`.cursor/plans/客户端安装包交付_33a8c0a6.plan.md`](../.cursor/plans/客户端安装包交付_33a8c0a6.plan.md) **仓库中不存在**；实现依据 `docs/modules/06-deployment.md`、`PROJECT_PROGRESS.md` §3.2、`docs/ACCEPTANCE.md` §S7
- `desktop-shell` 使用 Electron 28 + `contextIsolation`；staging 通过复制 `node_modules/electron/dist` 而非 electron-builder（MVP 简化）
- `server/.env.production` 仍为 `/prod-api`；客户端构建时由 `build-client.ps1` 注入 `VUE_APP_BASE_API=$CloudApiBase`

## 阻塞 / 待确认

- **无代码阻塞**（INFRA-A 已完成）
- **待用户确认：**
  1. 云端 API 稳定 HTTPS 基址（写入 `packaging/config/client.json.template` 或构建参数 `-CloudApiBase`）
  2. 本机是否已安装 NSIS（`makensis` 在 PATH）
  3. `Chrome-bin/` 是否随安装包分发（无内核时 `launchBrowser` 会报错）
  4. 关闭管理窗口后行为（托盘常驻 vs 一并退出）— ACCEPTANCE §S7 第 5 步尚未产品定夺

## 用户验收勾选

请在 [`ACCEPTANCE.md`](../ACCEPTANCE.md) §S7 完成实机验收后勾选：

| 检查项 | Agent-S7 自评 |
|--------|---------------|
| 安装包可安装 | ☐ 待实机（脚本就绪） |
| 桌面窗口启动 | ☐ 待实机 |
| 登录云端成功 | ☐ 待实机（需 CloudApiBase） |
| 创建/启动指纹环境 | ☐ 待实机（需 Chrome-bin） |
| 卸载保留用户数据 | ☑ NSIS 脚本已保留目录 |

**S7 阶段结论：** ☐ 通过 · ☐ 未通过 · ☑ **MVP 代码与打包脚本就绪，待实机验收**

## Agent 收尾 checklist

- [ ] 更新 `ACCEPTANCE.md` 对应 §（待用户实机验收后）
- [ ] 更新 `modules/06-deployment.md` 6.y.1 / 6.y.2 状态（建议 doc-sync）
- [ ] 更新 `INTEGRATION.md` NativeRT→Desktop 状态（建议 doc-sync）
- [x] 更新 `AGENT_COORDINATION.md` 认领表 → done
- [ ] 补建缺失的计划文件 `客户端安装包交付_33a8c0a6.plan.md`（可选 doc-sync）

## 交付物摘要

| 交付物 | 路径 |
|--------|------|
| Electron 桌面壳 | `desktop-shell/src/main.js`、`preload.js`、`native-ipc.js` |
| IPC smoke | `desktop-shell/scripts/smoke-test.js` |
| 打包编排 | `packaging/scripts/build-client.ps1` |
| NSIS 安装脚本 | `packaging/nsis/installer.nsi` |
| 客户端配置模板 | `packaging/config/client.json.template` |
| 验收报告 | `docs/acceptance-reports/S7-2026-07-11.md` |

## 如何验证（用户 / E2E Agent）

```powershell
# 1. IPC 桥接（无需 Electron）
node D:\bytesio\VirtualBrowser\desktop-shell\scripts\smoke-test.js

# 2. 安装 desktop-shell 依赖并启动窗口（需先构建 UI）
cd D:\bytesio\VirtualBrowser\server
npm install
npm run build

cd D:\bytesio\VirtualBrowser\desktop-shell
npm install
npm run start

# 3. 完整安装包（需 NSIS + Chrome-bin）
cd D:\bytesio\VirtualBrowser\packaging\scripts
.\build-client.ps1 -CloudApiBase "https://your-api.example.com"
# 运行 packaging\output\VirtualBrowser-Setup-0.1.0.exe
```
