# S5 验收报告 — Agent-Verify — 2026-07-11

## 范围

- **任务 ID：** S5-verify
- **认领表：** 见 [`AGENT_COORDINATION.md`](../AGENT_COORDINATION.md)
- **验收依据：** [`ACCEPTANCE.md`](../ACCEPTANCE.md) §S5（CRX 绑定 + launch 注入，模块 4.6/4.7）
- **变更文件：** 无代码变更（验收时在 `%LOCALAPPDATA%\VirtualBrowser\Extensions\` 写入测试 zip，属本地测试数据）

## 验证环境

| 项 | 实际 |
|----|------|
| 后端 | `http://localhost:3001` 正常 |
| 前端 + bridge | `http://localhost:9527` + dev-native-bridge 正常 |
| 内核 | `Chrome-bin/VirtualBrowser/146.0.7680.72/VirtualBrowser.exe` 存在 |
| 测试扩展 | 自建 `vb-verify-test.zip`（MV3 manifest，252 字节） |

---

## 验证记录（命令 + 实际输出）

### 检查项 1 — 环境表单可绑定插件

- **方法：** 源码审查 + 环境保存链路
- **预期：** 新建/编辑环境表单有多选「绑定插件」控件；保存时调用 `syncEnvCrxBindings`
- **实际：**
  - `server/src/views/browser/index.vue` 第 241–257 行：`el-select v-model="form.crxIds" multiple`
  - `server/src/api/native.js`：`addBrowser` / `updateBrowser` 均调用 `syncEnvCrxBindings(envId, item.crxIds)`
  - `handleCreate` / `handleUpdate` 均调用 `loadCrxOptions()` 加载可选插件列表
- **结果：** ☑ 通过（源码确认；未做浏览器表单交互目视复验）

### 检查项 2 — 保存后 crx-list 绑定正确

- **命令：**
  ```powershell
  node -e "
  const fs=require('fs');const path=require('path');
  const AdmZip=require('D:/bytesio/VirtualBrowser/server/node_modules/adm-zip');
  (async()=>{
    const zipPath=path.join(process.env.LOCALAPPDATA,'VirtualBrowser','Extensions','vb-verify-test.zip');
    const zip=new AdmZip();
    zip.addFile('manifest.json',Buffer.from(JSON.stringify({manifest_version:3,name:'VB Verify Test Extension',version:'1.0.0',action:{default_title:'VB Test'}},null,2)));
    zip.writeZip(zipPath);
    const login=await fetch('http://127.0.0.1:3001/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:'admin',password:'admin123'})}).then(r=>r.json());
    const auth={Authorization:'Bearer '+login.data.token,'Content-Type':'application/json'};
    const bridge=(n,p)=>fetch('http://127.0.0.1:9527/dev-native-bridge',{method:'POST',headers:auth,body:JSON.stringify({name:n,params:p})}).then(r=>r.text());
    const add=JSON.parse(await bridge('addLocalCrx',[zipPath]));
    const crxId=add.item.id;
    console.log('BIND', await bridge('syncEnvCrxBindings',['1',[crxId]]));
    console.log('CRX_LIST', fs.readFileSync(path.join(process.env.LOCALAPPDATA,'VirtualBrowser','User Data','crx-list.json'),'utf8'));
  })();
  "
  ```
- **预期：** `crx-list.json` 中对应插件 `environments` 含目标 envId
- **实际：**
  ```json
  {"ok":true,"changed":true}
  ```
  crx-list.json：
  ```json
  {
    "list": [{
      "id": "3a98de158c2e08bc",
      "name": "vb-verify-test",
      "enabled": true,
      "environments": ["1"],
      "path": "C:\\Users\\AAA\\AppData\\Local\\VirtualBrowser\\Extensions\\1783767375595-vb-verify-test.zip"
    }]
  }
  ```
- **结果：** ☑ 通过

### 检查项 3 — launchBrowser 加载扩展

- **命令：** 绑定后调用 `launchBrowser(['1'])`，检查 frontend 终端日志
- **预期：** 终端出现 `[dev-native-bridge] load-extension envId= ... paths= [...]`
- **实际（frontend 终端 734065.txt）：**
  ```
  [dev-native-bridge] load-extension envId= 1 paths= [
    'C:\\Users\\AAA\\AppData\\Local\\VirtualBrowser\\Extensions\\unpacked\\3a98de158c2e08bc'
  ]
  [dev-native-bridge] launchBrowser id= 1
  ```
- **spawn 参数佐证：** `native-bridge.js` 添加 `--load-extension=` 与 `--disable-extensions-except=`
- **结果：** ☑ 通过（终端日志确认）

### 检查项 4 — viewer 无插件上传/删除

- **方法：** 路由角色 + `v-permission` 源码审查
- **预期：** viewer 无法上传/删除/启用开关插件
- **实际：**
  - `server/src/router/index.js`：`/crx` 路由 `roles: ['admin', 'operator']` — **viewer 侧栏无插件管理入口**
  - `server/src/views/crx/list.vue`：上传按钮、启用开关、删除按钮均 `v-permission="['admin', 'operator']"`
  - viewer 角色登录 API 确认：`roles: viewer`
- **结果：** ☑ 通过（路由级拦截 + 组件级权限双重保障）

### 附加项 — chrome://extensions 目视确认（验收步骤第 4 步）

- **预期：** 在 Chrome 指纹窗口打开 `chrome://extensions` 可见已加载扩展
- **实际：** Agent 环境无法自动化操作已弹出的 VirtualBrowser.exe 内核窗口及其 `chrome://` 内部页
- **结果：** ☐ **blocked** — 需用户手动目视确认

---

## 与文档差异

- [`INTEGRATION.md`](../INTEGRATION.md) §CRX→Launch 仍标注「未 load-extension」— 与当前 `native-bridge.js` 实现不符，建议 Agent-Doc 后续同步（非本验收范围）

## 阻塞 / 待确认

| 项 | 已查文档 | 需用户确认 |
|----|----------|------------|
| chrome://extensions 扩展可见性 | ACCEPTANCE §S5 步骤 4 | 请用户启动 envId=1（已绑定 vb-verify-test），在指纹 Chrome 打开 `chrome://extensions`，确认「VB Verify Test Extension」已加载 |
| 浏览器表单/UI 目视 | modules/04、05 | 可选：admin 登录目视确认插件多选控件与上传按钮 |

## 用户验收勾选

请在 [`ACCEPTANCE.md`](../ACCEPTANCE.md) §S5 将 4 项必检 ☐ 改为 ☑。  
`chrome://extensions` 目视确认通过后，可将阶段结论改为 ✅ 通过；当前 Agent 判定为 **✅ 基本通过**（4/4 必检项 API/日志/源码通过，chrome 目视待用户补验）。

## Agent 收尾 checklist

- [x] 更新 `ACCEPTANCE.md` 对应 §
- [ ] 更新 `COMPAT_API.md` / `modules/XX`（不适用）
- [ ] 更新 `INTEGRATION.md` 衔接状态（建议后续同步 CRX→Launch 状态）
- [x] 更新 `AGENT_COORDINATION.md` 认领表 → done
- [ ] 更新 `docs/README.md` 模块状态列（不适用）
